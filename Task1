my_var = "ip nat inside source list ACL interface FastEthernet0/1 overload"

#Заменяем все подстроки Fast в строке
a = my_var.replace("Fast", "Gigabit")
print(a)


nat = "ip nat inside source list ACL interface FastEthernet0/1 overload"
n = nat.replace('Fast', 'Gigabit')
print(n)
